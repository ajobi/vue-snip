export { normalizeMaxLines } from './utils.normalize.lines'
export { normalizeSnipMethod } from './utils.normalize.method'
export { addObserver } from './utils.observer.add'
export { destroyObserver } from './utils.observer.destroy'
